package com.slatelog.slatelog.domain.event;

/**
 * Enum representing different voting options for a poll.
 * Options include "Yes," "No," and "Maybe."
 */
public enum VoteOption {
    Yes,
    No,
    Maybe
}
